package com.tripmate.shared.data

// Superseded: each entity's toDomain() mapping now lives next to the
// repository that uses it (FirestoreTripRepository.kt, FirestoreActivityRepository.kt,
// FirestorePackingRepository.kt, FirestoreBudgetRepository.kt,
// FirestoreDocumentRepository.kt) instead of one shared file, per the
// feature-first colocation described in CLAUDE.md. This file is kept only
// so it doesn't look deleted-by-accident; it can be removed once you're
// comfortable it's unused.
